<?php
session_start();
global $class;
global $term;
$_SESSION['class']=$_GET['Print'];
$_SESSION['term']=$_GET['Term'];
if(isset($_SESSION['role'])){
  require_once('Database.php');
   $term=$_GET['Term'];
  $db=new Database();
	include('include/fpdf.php');
	class myClass extends FPDF{
    public $angle;
    public function Header(){
      $database=new Database();
      $details=$database->getSchool();
      foreach ($details as $key) {
        $this->SetFont('Arial','B','40');
        if($database->checkWatermark()){
          $this->watermark($key->sName,45,11);
        }else{
          continue;
        }
        $this->SetFont('Arial','B','10');
      }

    }
    function watermark($text,$angle,$start){
      $this->SetTextColor(255,200,233);
      $this->Rotate($angle,20,200);
      $this->Text($start,190,$text);
      $this->Rotate(0);
    }
    function Rotate($angle,$x=-1,$y=-1)
    {
      if($x==-1)
        $x=$this->x;
      if($y==-1)
        $y=$this->y;
      if($this->angle!=0)
        $this->_out('Q');
      $this->angle=$angle;
      if($angle!=0)
      {
        $angle*=M_PI/180;
        $c=cos($angle);
        $s=sin($angle);
        $cx=$x*$this->k;
        $cy=($this->h-$y)*$this->k;
        $this->_out(sprintf('q %.2F %.2F %.2F %.2F %.2F %.2F cm 1 0 0 1 %.2F %.2F cm',$c,$s,-$s,$c,$cx,$cy,-$cx,-$cy));
      }
    }
    function _endpage()
    {
      if($this->angle!=0)
      {
        $this->angle=0;
        $this->_out('Q');
      }
      parent::_endpage();
    }
    function head($db){
      $detail=$db->getSchool();
      foreach($detail as $key){
			$this->SetFont('Arial','',10);
			//$statement=$db->displayFees($class,$term);
				$this->Image('assets/images/'.$key->Image,20,15,0,20);
				$this->SetFont('Courier','B','20');
				$this->Cell(200,20,$key->sName,0,0,'C');
        $this->Ln(15);
        $this->SetFont('Courier','B','10');
				$this->Cell(200,5,"Tel :".$key->Phone,0,0,'C');
        $this->Ln();
        $this->SetFont('Courier','B','10');
				$this->Cell(200,5,"P.O.Box ".$key->pBox." ".$key->pCode." ".$key->pCity." Kenya",0,0,'C');
				$this->Ln(10);
        $this->SetFont('Arial','B','10');
				$this->Cell(200,5,"Form ".$_SESSION['class']." Next Term   Fee Structure",0,0,'C');
				$this->Ln(10);
      }
		}
		function footer(){
			$this->SetY(-15);
			$this->SetFont('Arial','','10');
      $this->Cell(0,10,'=======================================================================================',0,0);
      $this->Ln(3);
			$this->Cell(0,10,'The School holds the right to edit the contents of this fees structure when necessary',0,0);
			$this->SetX(-40);
      $this->Ln();
      $this->SetTextColor(125,125,125);
			$this->Cell(0,0,'Generated By '.$_SESSION['role'],0,0);
		}
		function headerTable(){
			$this->SetFont('Arial','B','12');
			$this->Cell(100,10,'Votehead',1,0,'C');
			$this->Cell(95,10,'Amount',1,0,'C');
			$this->Ln();
		}
		function viewTable($db){
			$this->SetFont('Arial','',10);
			$statement=$db->displaynTFees($_SESSION['class'],$_SESSION['term']);
      $total=0;
      foreach ($statement as $data) {
				$this->Cell(100,10,$data->votehead,1,0,'C');
				$this->Cell(95,10,$data->amount,1,0,'C');
        $this->Ln();
        $total=$total+$data->amount;
			}
      	$this->Ln();
      $this->SetTextColor(255,0,0);
      $this->Cell(100,10,"Total ",0,0,'C');
      $this->Cell(95,10,"=====",0,0,'C');
      $this->Ln(3);
      $this->Cell(100,10,"",0,0,'C');
      $this->Cell(95,10,$total,0,0,'C');
      $this->Ln(3);
      $this->Cell(100,10," ",0,0,'C');
      $this->Cell(95,10,"=====",0,0,'C');
		}
	}
	$pdf=new myClass();
	$pdf->AliasNbPages();
	$pdf->AddPage('p','A4',0);
	$pdf->head($db);
	$pdf->headerTable();
	$pdf->viewTable($db);
	$pdf->output();
}else{  header("Location: index.php");}
?>
